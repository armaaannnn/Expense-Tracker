<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.expensetrackerarihant"> <!-- Replace with your actual package name -->

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExpenseTrackerArihant"
        tools:targetApi="31">

        <!-- Logo/Splash Screen Activity -->
        <activity android:name=".LogoScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />

        <activity android:name=".CreateAccountActivity" />


        <activity android:name=".DashboardActivity" />

        <activity android:name=".AddExpenseActivity"/>

        <activity android:name=".ReminderActivity" />

        <activity android:name=".StatisticsActivity"/>

        <activity android:name=".ProfileActivity"/>



    </application>

</manifest>
